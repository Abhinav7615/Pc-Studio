const fs=require('fs'); const path=process.argv[2]; const s=fs.readFileSync(path,'utf8'); let stack=[]; for(let i=0;i<s.length;i++){ const c=s[i]; if(c==='(') stack.push(i); else if(c===')'){ if(stack.length===0){ const lines=s.slice(0,i).split(/\r?\n/); const line=lines.length; const col=lines[lines.length-1].length+1; console.log('Unmatched ) at', {line,col}, 'context:', s.slice(Math.max(0,i-40), i+40).replace(/\n/g,'\\n')); break;} else stack.pop(); }} console.log('done');
