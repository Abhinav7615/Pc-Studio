const fs=require('fs'); const [,,path,line,col]=process.argv; const s=fs.readFileSync(path,'utf8'); const lines=s.split(/\r?\n/); let idx=0; for(let i=0;i<line-1;i++) idx+=lines[i].length+1; idx+=Number(col)-1; const start=Math.max(0,idx-40); const end=Math.min(s.length, idx+40); const substr=s.slice(start,end); console.log('substr:"'+substr.replace(/\n/g,'\\n')+'"'); console.log('chars:'); for(let i=0;i<substr.length;i++){ const ch=substr[i]; console.log(i+start, ch, ch.charCodeAt(0)); }